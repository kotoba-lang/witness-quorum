{
  "name": "witness-quorum-nbb",
  "version": "0.1.0",
  "private": true,
  "description": "npm deps for the nbb (ClojureScript-on-Node) sibling files (signer.cljs/selector.cljs/attestation.cljs) -- the .clj files are pure JVM and have no npm deps of their own.",
  "dependencies": {
    "@noble/curves": "^1.7.0"
  }
}
